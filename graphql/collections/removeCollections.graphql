mutation RemoveCollection($collectionId: Float!) {
  removeCollection(collectionId: $collectionId) {
    banner
    description
    id
    isActive
    isFeatured
    name
    slug
    listings {
    ...ListingFields 
    }
  }
}