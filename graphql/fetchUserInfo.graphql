query GetUserById($userId: Float!) {
  getUserById(userId: $userId) {
    createdAt
    email
    facebookId
    followerCount
    followingCount
    followingStatus
    id
    isEmailVerified
    isStaff
    listingCount
    isApproved
    pointBalance
    profile {
      address
      avatar
      country
      city
      avatarThumbnail
      facebookHandle
      googleHandle
      id
      name
      instagramHandle
      isPhoneNumberVerified
      linkedinHandle
      phoneNumber
      twitterHandle
      zipCode
      state
    }
    roles {
      id
      name
      description
    }
    username
  }
}